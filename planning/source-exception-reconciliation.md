# Five guarded source bindings before graph contraction

Read-only production D1 check on 26 September 2026. These are the only five known canonical poems whose current source binding cannot yet be derived from the established identity copy. No row in this file authorizes a write before the publication audit, private R2 archive, disposable restore rehearsal, and direct-source deployment have passed. Re-read every precondition immediately before repair.

The direct writer hashes `canonicalJson({content: currentArabicLines, titleArabic: currentTitle})` with SHA-256. The hashes below were computed from the actual canonical `poem` rows; no poem text was stored locally. For the three equal rows, that hash equals the staged revision/import hash. The two different rows must first bind their **current** canonical content, then be recrawled through the direct writer with that hash as its CAS precondition.

| Canonical poem | Source ID | Author | Current hash | Staged hash | Arabic relation | Visible snapshot at check |
|---|---:|---|---|---|---|---|
| `4df35b2d-55bc-4bee-b202-a08b74f26941` | 22937 | `34b09bf6-a530-464f-8581-eb76983581e7` | `aec08129eeeedcfe956891604ae5be9449f569e8a2a4d91a54f27c737c0bd283` | same | title and 16 lines equal | yes |
| `cc065d9e-686b-4506-8826-56fb7130d781` | 65608 | `8ed34cd4-16a5-48e5-aff1-3a7f65f54d30` | `50e5fdd9638b87eb49f6a71edf4c58667862d72b76ada7587c9490142b3c4af0` | same | title and 8 lines equal | no |
| `eaa1c6cb-402f-4e5d-bf1f-f13569483608` | 95050 | `a0407c81-0cda-4587-9ba0-642d17439f53` | `ea2e1004fb317ed60e3fcfd76f93820c95f9ee66349316b53c923c9d6af55a3b` | `b0382614f32f94d5745257acea306e932518910916d739056994bb49779a2ce4` | title equal, 50 lines differ | no |
| `c3718eff-15af-46bf-9258-a843d5222181` | 95053 | `a0407c81-0cda-4587-9ba0-642d17439f53` | `918967bfbc3dfc53b5eae2f4b1f3a814cecc9481902020e395c7a9573aec9cce` | same | title and 2 lines equal | pending backfill at check |
| `d2ad44f1-577c-456a-a1be-fe0d57e52daa` | 95064 | `a0407c81-0cda-4587-9ba0-642d17439f53` | `926aedaf43967e7c4457a64f2cdba70d33f0bffc773a3696614e83ab251756c5` | `92f07d65889e7630229d7694625ddbc00cba0ee4d9547035641d458478df03e6` | title equal, 70 lines differ | no |

The first three already have `source_name='aldiwan'`, the matching numeric `source_poem_id`, source URL, and `source_version=1`; only `source_hash` is null. The last two have null source name/key/URL, version 0, and sealed import records that point to these exact canonical IDs. Their source URLs are `https://www.aldiwan.net/poem95053.html` and `https://www.aldiwan.net/poem95064.html`. All five have `source_hash=NULL`; the currently public translation on 22937 must remain visible. If 95053 gains a valid inactive publication during backfill, preserve it too.

Repair with one compare-and-swap per row. Before each write, read its current title, full Arabic JSON, author, source columns, publication JSON/hash, and source identity or sealed import record; recompute the current hash and require the exact pinned value above. Include the **exact current title and Arabic JSON** in the SQL `WHERE` clause, as well as ID, author, null source hash, expected source key/version, and the source identity/import relationship. A changed row must update zero rows and stop the run. For the mapped three, set only `source_hash`. For the sealed two, set source name, numeric poem ID, URL, `source_hash`, version 1, and collection time. If a row has a valid audited snapshot for the same current Arabic, set `publication_source_hash` to the same current hash in that CAS so it does not enter the paid queue merely because its former source hash was null. Never overwrite `publication_json`, translation fields, title, Arabic, slug, or author in this repair.

`python3 typescript/scripts/reconcile-source-bindings.py` is a read-only live preflight. After the archive and disposable restore rehearsal pass, run it with `--execute --archive-manifest saqi-corpus-archive/d1/<key>/manifest.json` while collection and the rig remain disabled. It verifies the production D1 UUID, archive counts, all five current Arabic hashes/provenance edges, and each exact CAS; it stops on any changed precondition. The 26 September read-only rehearsal passed all five rows without writing. A partial execution is intentionally fail-closed; inspect the printed completed IDs and current rows before rerunning, since it will refuse an already-bound row.

After all five CAS updates, require exactly five affected rows, five unique `(source_name, source_poem_id)` bindings, zero FK errors, unchanged author/poem counts, unchanged public URL responses and publication hashes. Recrawl 95050 and 95064 from source using the direct writer. It must update the **same** poem IDs through expected-hash CAS if the live Arabic differs. If the source still matches the staged version, require the resulting hash to match the staged hash above. Reaudit their public pages and ensure changed Arabic becomes due for a new translation. Do not infer or bulk merge any of the other 26,049 source-unmapped poems from slugs; 3,968 slug inferences collide with a different canonical row.

Execution completed on 26 September after the full D1 restore and public-reader cutover. All five CAS updates changed exactly one row; canonical IDs, 1,391-author/104,960-poem counts, and zero FK errors verified afterward. Existing snapshots on 22937 and 95053 were preserved. The source-history tables are no longer needed to resolve these bindings. The two differing source texts can be recrawled through their established canonical keys after contraction; their old staged history is non-core and remains in the private archive.
